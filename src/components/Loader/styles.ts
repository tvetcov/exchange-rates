import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  progress: {
    position: 'fixed',
    top: '92vh',
    right: '3vw',
  },
}));

export default useStyles;
